# OIBSIP_PYTHONPROGRAMMING_TASK3
This Python program generates a **random password** based on user preferences. The user enters the desired password length and chooses whether to include letters, numbers, and symbols. The program uses the **random** and "string" modules to select characters and create a secure password, then displays the generated password.
