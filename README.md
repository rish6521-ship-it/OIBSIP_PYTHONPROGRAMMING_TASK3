
Features:
 Allows user to choose password length
 Option to include letters, numbers, and symbols
 Generates a random and secure password
 Uses user preferences to customize the password
 Displays the generated password instantly
 Shows error message if no character type is selected

Tools Used:
 Python programming language
 Python IDLE (to run the program)
 Python libraries: random and string
 Built-in functions: input(), print()
 Conditional statements (if, else)
 Loop (for loop)
